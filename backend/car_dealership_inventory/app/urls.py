from django.urls import path

# from .views import vehiclesDetailView, vehiclesListView, vehiclesPurchaseView, vehiclesRestockView

urlpatterns = [
    # path('vehicles/', vehiclesListView.as_view()),
    # path('vehicles/<int:pk>/', vehiclesDetailView.as_view()),
    # path('vehicles/<int:pk>/purchase/', vehiclesPurchaseView.as_view()),
    # path('vehicles/<int:pk>/restock/', vehiclesRestockView.as_view()),
]
